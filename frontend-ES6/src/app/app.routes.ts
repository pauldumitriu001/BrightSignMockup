import { Routes } from '@angular/router';
import { DeviceInfoComponent } from './device-info/device-info.component';

export const routes: Routes = [
  { path: '', component: DeviceInfoComponent }, // Default route
];
